import React from "react";
import ReactDOM from 'react-dom';

const root = document.getElementById("root");

ReactDOM.render(
    <React.StrictMode>
        <div>
            FUCK
        </div>
    </React.StrictMode>,
root);